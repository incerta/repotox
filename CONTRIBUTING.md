# Development Prerequisites

Tests use [`mongodb-memory-server`](https://github.com/typegoose/mongodb-memory-server),
which downloads and starts an in-memory MongoDB instance automatically.
No external database setup is required — just run:

```sh
npm test
```
